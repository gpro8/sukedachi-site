# Optional Polygon RPC for site + 加勢仲間 getLogs (recommended in production).
# Public nodes prune history / rate-limit under multi-user load.
# Prefer Alchemy Polygon Mainnet (separate app from Base if needed):
#   https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
# Or QuickNode / LlamaNodes Polygon HTTPS.
# VITE_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
#
# Local only — never commit real keys. File is gitignored via .env / .env.*
